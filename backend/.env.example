# Django Secret Key (generate a random 50+ char secret in production)
SECRET_KEY=django-insecure-&ls)i1wafb#%71#ob!j&qbpps^h7jqq6+=sjw3#jis4x(wqs+k

# Set to False in production!
DEBUG=True

# Comma-separated allowed hosts
ALLOWED_HOSTS=localhost,127.0.0.1,testserver

# Comma-separated allowed CORS origins
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Database configuration (leave blank or sqlite:// for default SQLite, or postgres://user:pass@host:5432/dbname)
DATABASE_URL=

# Email configuration (console backend for local testing, smtp for production)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=Cabinet Medical <noreply@cabinetmedical.local>
